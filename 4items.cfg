# tfm2_item_tactics 아이템 칸 수 설정
# slots = 4  →  아이템 4칸 (구매/전술/경기중 UI 전부 4칸, 4번째 신경망 자동추천)
# slots = 3  →  아이템 3칸 (바닐라 그대로, 4-아이템 기능 전부 OFF)
# ※ 변경 후 게임 재시작 필요
# tfm2_item_tactics - item slot count
# slots = 4  ->  4 item slots (purchasing / tactics screen / in-match UI all use 4; the 4th is auto-recommended by the neural network)
# slots = 3  ->  3 item slots (vanilla behaviour; every 4-item feature is OFF)
# Note: a game restart is required after changing this.
slots = 3
